# Flash Timer

シンプルな1画面タイマー（設定 / 作動 / タイムアップ）。

## ローカルで起動

- `index.html` をブラウザで開くだけです。

## GitHub Pages で公開

1. GitHub のリポジトリ画面で `Settings`
2. 左メニュー `Pages`
3. `Build and deployment` → `Source` を `Deploy from a branch`
4. `Branch` を `main` / `(root)` にして `Save`

反映後、表示されるURLにアクセスすると公開版が見られます。

